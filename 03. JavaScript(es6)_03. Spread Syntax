## 1. 나머지 매개변수

function merge(msg1,msg2) {
    return msg1+msg2;
}

console.log(merge('안녕하세요')); //하나만전달하면나머지는undefined가나옴
console.log(merge('안녕하세요', '반갑습니다'));
console.log(merge('안녕하세요', '반갑습니다', '제 이름은 홍길동입니다'));
--------------------------------------------------------------------------
나머지 매개변수 …을 이용하면 인자가 한 개 이상이더라도 매개변수를 한 곳에 모아 배열에 담아 가져올 수 있음
→ mergeAll에 배열 형태로 args에 담아오는 것


function mergeAll(...args) {

    let message = '';

    for(let arg of args) message +=arg;

    return message;
}
--------------------------------------------------------------------------

유의할 점 : 나머지 매개변수는 항상 마지막에 있어야 함

function func(arg1, arg2, ...args) {} 

function func(arg1, ...args, arg2) {} ← 이 경우엔 오류가 남

## 2. 스프레드 문법, 전개 문법


console.log(Math.max (10, 30, 20)); //Math라는class에서제공해주는메서드

let arr = [10, 30, 20];
console.log(Math.max(arr)); //배열이아니라숫자목록으로써야만받을수잇음

console.log(Math.max(...arr)); //배열을인수로펼쳐서전달해줌
--------------------------------------------------------------------------

Math.max()는 배열이 아닌 숫자 목록을 인수로 받기 때문에 배열의 경우 원하는 대로 동작 x

→ 스프레드 문법을 사용함
- 스프레드 문법 : 나머지 매개변수처럼 목록을 배열로 가져오는 것과 반대로 배열을 통째로 매개변수에 넘겨주는 기능!! 하나로 뭉쳐있는 여러 값들을 개별적인 값들의 목록으로 만듦


let arr1 = [10, 30, 20];
let arr2 = [100, 300, 200];

// 배열 객체 여러 개 전달 가능
console.log(`가장 작은 값 : ${Math.min(...arr1, ...arr2)}`);
// 일반 값과 혼합해서 사용 가능
console.log(`가장 작은 값 : ${Math.min(1, ...arr1, ...arr2, 3)}`);

// 배열 병합에도 사용 가능 concat보다 간결
let merged = [0, ...arr1, 2, ...arr2];
console.log(merged);
--------------------------------------------------------------------------
- 이터러블 배열 변환

let str="javascript";
console.log([...str]);       // for...of와 같은 방식으로 문자열을 배열로 변환
// let for strs of str {

// }
console.log(Array.from(str));
// 포오브써도되고문자열로써도됨
--------------------------------------------------------------------------

포인문은 갖고있는 프로퍼티 키를 이용해서 순회함 → key가 나옴

포오브는 배열 안에ㅔ 있는 요소나 배열을 이터러블 → 값이 나옴
